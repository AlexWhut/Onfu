# Enforce sensible line endings for text files in this repo
* text=auto

# Ensure Firestore rules use LF in the repository and on checkout
*.rules text eol=lf

# Common source files: keep LF in repo
*.kt text eol=lf
*.java text eol=lf
*.xml text eol=lf
*.gradle text eol=lf
*.kts text eol=lf

# Shell scripts should keep LF
*.sh text eol=lf
